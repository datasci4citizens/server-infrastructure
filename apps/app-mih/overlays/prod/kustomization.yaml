apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: app-mih

resources:
  - ../../base

# No secret: the SPA has no runtime secrets (env baked at build time).

# Image tag updated by CI workflow on each build.
images:
  - name: ghcr.io/datasci4citizens/app-mih
    newTag: sha-f033462
