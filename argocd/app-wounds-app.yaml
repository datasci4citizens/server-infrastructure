apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-wounds
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/datasci4citizens/server-infrastructure.git
    targetRevision: main
    path: apps/app-wounds/overlays/prod
  destination:
    server: https://kubernetes.default.svc
    namespace: app-wounds
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
